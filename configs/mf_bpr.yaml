experiment:
  name: "mf_bpr_movielens"
  seed: 42

data:
  dataset: "movielens-100k"
  implicit: true
  negatives: 4

model:
  type: "MF"
  embedding_dim: 64

train:
  epochs: 20
  lr: 0.001
  batch_size: 2048
  weight_decay: 1e-5

eval:
  k: [5, 10, 20]
  metrics: ["recall", "ndcg"]
